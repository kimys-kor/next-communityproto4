export const metadata = {
  title: "꽁머니팡: 유머 이슈 게시판",
  description: "유머 이슈",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <div children={children} />;
}
