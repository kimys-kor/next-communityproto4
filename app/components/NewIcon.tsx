import Image from "next/image";

const NewIcon = () => {
  return (
    <span className="flex justify-center items-center w-4 h-4 bg-rose-500 text-white text-xs rounded-sm p-2">
      N
    </span>
  );
};

export default NewIcon;
